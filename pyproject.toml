[project]
name = "nsm-dna"
version = "0.1.0"
description = "Explore Next-Scale Modeling applied to genomic sequence data."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "datasets>=4.5.0",
    "openpyxl>=3.1",
    "pyarrow>=25.0.0",
    "tqdm>=4.67.3",
]
